spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      server:
        webflux:
#          discovery:
#            locator:
#              enabled: true
#              lower-case-service-id: true
          routes:
            - id: customer_service_route
              uri: http://localhost:8000
              predicates:
                - Path=/customer-service/**
              filters:
                - StripPrefix=1
            - id: car_service_route
              uri: http://localhost:8100
              predicates:
                - Path=/car-service/**
              filters:
                - StripPrefix=1
            - id: renting_service_route
              uri: http://localhost:8200
              predicates:
                - Path=/renting-service/**
              filters:
                - StripPrefix=1
server:
  port: 8765
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
#
#  endpoint:
#    gateway:
#      enabled: true
#      access: unrestricted